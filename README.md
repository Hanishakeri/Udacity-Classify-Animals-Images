# Udacity-Classify-Animals-Images
The classify pet images is a python project from the Udacity AI Programming with Python Nanodegree. It uses a convolutional neural network (CNN) to classify the pet images.
